---
import { t } from '../i18n/translations';
import { type Locale } from '../i18n/utils';

interface Props {
  locale?: Locale;
}

const { locale = 'zh' } = Astro.props;
---

<footer class="py-12 border-t border-white/5 mt-10">
  <div class="max-w-[800px] mx-auto px-6 flex flex-col items-center">
    <div class="flex gap-6 mb-6">
      <a class="text-gray-500 hover:text-white transition-colors text-sm" href="#">{t('footer.social.twitter', locale)}</a>
      <a class="text-gray-500 hover:text-white transition-colors text-sm" href="#">{t('footer.social.github', locale)}</a>
      <a class="text-gray-500 hover:text-white transition-colors text-sm" href="#">{t('footer.social.email', locale)}</a>
    </div>
    <p class="text-xs text-gray-600">{t('footer.copyright', locale)}</p>
  </div>
</footer>
